/* const car={name : 'soto car',color : 'green',price : 20000};
console.log(car);
const carStringfy =JSON.stringify(car);
console.log(carStringfy); */

/* const student ={
    id : 29,
    name: 'razob',
    subject : 'cse',
    subjectName :['java','c','python'],
        teacher :{css : 'fla',java : 'babab'},
        classOpen : true
};

const studentStringfy =JSON.stringify(student);
console.log(studentStringfy);
const studentParse =JSON.parse(studentStringfy);
console.log(studentParse); */

/* function lodeUser(){
    fetch('https://jsonplaceholder.typicode.com/users')
    .then(Response => Response.json())
    .then(user => usershow(user))
}

function usershow(userData){
    const ul =document.getElementById('users');
    for(const user of userData ){
        const li=document.createElement('li')
        li.innerText = `id : ${user.id} , ${user.name}`
        ul.appendChild(li)

    }
    
} */
 